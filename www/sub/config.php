<?php
/**************************************************
 * This is the standard config file
 **************************************************/

// standard variables that are needed across the project
error_reporting(1);
$dev = true;


// so links can change depending on where the server is
$host = "192.168.1.122";//"localhost";//"10.5.1.15";
// full urls are the way to go(shorthand urls are easily broken and can cause security issues)

// some important variables
$signup = true;
$login = true;

$num_post = 10;


?>